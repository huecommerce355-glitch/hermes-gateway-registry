# HACP Register Extension

An extension registration must include `name`, `version`, structured `protocol`, `lifecycle`, `owner`, `boundaries`, unique prefixes, operations, and a safe read-only `probe_cmd`. Validate the complete candidate with `validate_registry.py` before adding it to the registry.

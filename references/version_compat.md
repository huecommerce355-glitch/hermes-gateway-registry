# Version Compatibility

HACP compatibility compares structured `protocol.name` and semantic `protocol.version` major/minor values. Names are case-insensitive. Different major versions are incompatible; equal major versions are compatible. Display strings such as `HACP-v1.0` are presentation only.
